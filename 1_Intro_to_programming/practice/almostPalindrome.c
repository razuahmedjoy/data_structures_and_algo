#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main()
{

    int t;
    scanf("%d", &t);
    while(t--){
        char word[1001];
        scanf("%s", word);
        int count = 0;
        int j = strlen(word);

        

        printf("%d\n", count);
    }

    return 0;
}


// not solved fully